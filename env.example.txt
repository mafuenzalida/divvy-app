# Divvy environment variables
# Rename this file to .env and fill in your values

# ===================
# OCR (pick one)
# ===================

GEMINI_API_KEY=your_gemini_api_key_here
# or
GOOGLE_API_KEY=your_google_api_key_here

# OpenAI API (optional, costs money)
# OPENAI_API_KEY=sk-your-openai-key

# ===================
# Database (Turso)
# ===================

TURSO_DATABASE_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token_here

# ===================
# Web / CORS / auth
# ===================

# Public URL of the app (embedded in magic-link emails — must match the origin users open)
BASE_URL=http://localhost:8000

# Comma-separated browser origins allowed to call the API with credentials (include the same host as BASE_URL)
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Optional: future session signing (set in production)
# SESSION_SECRET=

# ===================
# Email (magic link) — Resend only
# ===================
#
# 1) resend.com → API Keys → create key → MAIL_API_KEY
# 2) Domains → verify domain → use an address on that domain in MAIL_FROM (see Resend “from” examples)
# 3) BASE_URL (above) must be the public app URL (scheme + host [+ port]) so the link in the email lands on this app; after login, users are sent to /app
#
MAIL_PROVIDER=resend
MAIL_API_KEY=
MAIL_FROM=

# Comma-separated admin emails (signed-in users in this list can call /api/admin/bills). Use the same email as MIGRATE_OWNER_EMAIL if you ran scripts/migrate_bills_owner.py
ADMIN_EMAILS=

# ===================
# Payments
# ===================

FINTOC_USERNAME=

# ===================
# Notes
# ===================
# - If Turso vars are not set, the app falls back to data/bills.json
# - Magic links require MAIL_API_KEY and MAIL_FROM (Resend); see README “Email sign-in (Resend)”
# - See MIGRATIONS.md for URL and schema changes
